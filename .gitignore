# SSL 개인키 평문 - 절대 포함 금지
ssl/*.key
ssl/*.pem

# 암호화된 파일은 포함 (*.enc)
!ssl/*.enc
!nextcloud/*.enc

# 임시파일
*.swp
*.bak
.DS_Store

# DB 원본 SQL (너무 크면 제외, .gz 압축본만 포함)
# db/*.sql  ← 주석처리: .gz 압축본만 포함

# 로그
*.log
